name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Checkout Pi workspace dependency
        run: |
          PI_REPO_REF="${PI_REPO_REF:-v0.80.3}"
          git clone --depth 1 --branch "$PI_REPO_REF" https://github.com/earendil-works/pi.git ../pi \
            || git clone --depth 1 --branch v0.80.3 https://github.com/earendil-works/pi.git ../pi
      - name: Install dependencies
        run: npm ci
      - name: Lint, build, and test
        run: npm test
