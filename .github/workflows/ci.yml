name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  upstream-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint, build, and test
        run: npm test
      - name: Assert isolated standard Pi resolution
        run: |
          node --input-type=module -e '
            for (const specifier of ["@earendil-works/pi-ai", "@earendil-works/pi-coding-agent", "@earendil-works/pi-tui"]) {
              const resolved = await import.meta.resolve(specifier);
              if (!resolved.startsWith(`file://${process.cwd()}/node_modules/`)) throw new Error(`${specifier} resolved to ${resolved}`);
              console.log(`${specifier}: ${resolved}`);
            }
          '

  fork-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Checkout Pi ModelRuntime contract source
        run: |
          git clone --filter=blob:none https://github.com/mizuikki/pi.git ../pi
          git -C ../pi checkout --detach 62cf438762cc577fae5a9691cb0c4d1824b8998f
          test "$(git -C ../pi rev-parse HEAD)" = "62cf438762cc577fae5a9691cb0c4d1824b8998f"
      - name: Build isolated Pi tarballs and test the project copy
        run: PI_FORK_DIR=../pi PI_FORK_REF=62cf438762cc577fae5a9691cb0c4d1824b8998f npm run test:pi-fork

  package-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Verify clean package installation and consumer types
        run: npm run test:package-smoke
